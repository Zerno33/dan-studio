export default function Home() {
  return (
    <main style={{ fontFamily: "monospace", padding: 40, background: "#0A0A0A", color: "#EDEDED", minHeight: "100vh" }}>
      <h1 style={{ color: "#E5152A" }}>PROMPT_ENGINE — skeleton deploy</h1>
      <p>MYS-11: pierwszy preview aplikacji. Konsola produkcyjna (MYS-43) jeszcze nie wgrana.</p>
      <p>API endpoints żywe pod /api/generate i /api/admin/*.</p>
    </main>
  );
}
